(()=>{const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=n=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:0}).format(Number(n||0));const stars=n=>{const x=Math.max(0,Math.min(5,Math.round(Number(n||0))));return '★'.repeat(x)+'☆'.repeat(5-x)};function storyCard(x){return `<article class="proof-card"><span class="badge">${esc(x.category||'Confirmed outcome')}</span><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p><div class="story-values">${x.valueSaved?`<span><small>Value saved</small><b>${money(x.valueSaved)}</b></span>`:''}${x.debtReduced?`<span><small>Debt reduced</small><b>${money(x.debtReduced)}</b></span>`:''}${x.assetValueProtected?`<span><small>Assets protected</small><b>${money(x.assetValueProtected)}</b></span>`:''}${x.taxExposureReduced?`<span><small>Tax exposure reduced</small><b>${money(x.taxExposureReduced)}</b></span>`:''}${x.jobsProtected?`<span><small>Jobs protected</small><b>${Number(x.jobsProtected)}</b></span>`:''}${x.daysToResolution?`<span><small>Resolution time</small><b>${Number(x.daysToResolution)} days</b></span>`:''}</div></article>`}function proCard(p){return `<article class="proof-card"><span class="badge">${esc(p.membershipTier)}</span><h3>${esc(p.name)}</h3><p><b>${esc(p.profession)}</b></p><p>${esc(p.specialities||'Verified professional in The Chancellor’s Professional Network.')}</p><div class="pro-score">${Number(p.performanceScore||0)}</div><small>Chancellor Performance Score</small>${p.rating?`<div class="stars">${stars(p.rating)}</div><small>${Number(p.rating).toFixed(1)} from ${Number(p.ratings||0)} client rating${Number(p.ratings||0)===1?'':'s'}</small>`:''}<p><small>${esc(p.regions||p.country||'South Africa')}</small></p></article>`}function testimonialCard(t){return `<article class="proof-card"><div class="stars">${t.rating?stars(t.rating):''}</div><h3>“${esc(t.quote)}”</h3><small>${esc(t.attribution||'Verified Rescue client')}</small></article>`}async function load(){try{const r=await fetch('/api/public/proof');const d=await r.json();if(!r.ok)throw new Error(d.error||'Results could not be loaded.');const m=d.metrics||{};$('#proofConfirmed').textContent=Number(m.confirmedOutcomes||0);$('#proofValue').textContent=money(m.totalValueSaved);$('#proofDebt').textContent=money(m.debtReduced);$('#proofDays').textContent=`${Number(m.averageDaysToResolution||0)} days`;$('#proofAssets').textContent=money(m.assetValueProtected);$('#proofTax').textContent=money(m.taxExposureReduced);$('#proofJobs').textContent=Number(m.jobsProtected||0);$('#proofStories').innerHTML=(d.stories||[]).length?d.stories.map(storyCard).join(''):'<div class="proof-empty">Verified success stories will appear here only after a client-confirmed outcome has been separately approved for public publication.</div>';$('#proofPros').innerHTML=(d.professionals||[]).length?d.professionals.map(proCard).join(''):'<div class="proof-empty">Public professional profiles will appear here only for verified active members approved for public listing.</div>';$('#proofTestimonials').innerHTML=(d.testimonials||[]).length?d.testimonials.map(testimonialCard).join(''):'<div class="proof-empty">Client testimonials will appear here only after written feedback has been approved for public use.</div>';$('#proofMethod').textContent=d.methodology||'';}catch(err){$('#proofStories').innerHTML=`<div class="proof-empty">${esc(err.message)}</div>`;}}load();})();