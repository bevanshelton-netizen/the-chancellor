(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));const when=s=>{if(!s)return'—';const d=new Date(s);return Number.isNaN(d.getTime())?esc(s):d.toLocaleString('en-ZA',{dateStyle:'medium',timeStyle:'short'})};const money=n=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:2}).format(Number(n||0));
function billingMarkup(b,caseId,role){const pro=role==='professional'||role==='admin',client=role==='client';const quotes=b.quotes||[],invoices=b.invoices||[],payments=b.payments||[];return `<section class="case-room-panel case-billing-panel"><div class="case-room-head billing-head"><div><p class="eyebrow">Case billing</p><h3>Fees, approvals & payments</h3><p class="room-role-note">Professional fees and Chancellor platform fees stay visible in the case record. Payouts are only recorded after admin review.</p></div><span class="badge">${Math.round(Number(b.platformFeeRate||0)*100)}% platform fee</span></div><div class="billing-summary"><span><small>Client paid</small><b>${money(b.summary?.clientPaid)}</b></span><span><small>Platform revenue</small><b>${money(b.summary?.platformRevenue)}</b></span><span><small>Professional outstanding</small><b>${money(b.summary?.professionalOutstanding)}</b></span></div>${pro?`<form class="room-form room-quote-form"><h4>Issue professional fee quote</h4><input name="title" required placeholder="Service / intervention"><textarea name="description" rows="3" placeholder="Scope of work"></textarea><div class="billing-two"><input name="amount" type="number" min="1" step="0.01" required placeholder="Total client fee"><input name="depositAmount" type="number" min="1" step="0.01" placeholder="Deposit amount"></div><button class="button compact">Issue quote</button><p class="status"></p></form>`:''}<div class="billing-quotes"><h4>Quotes & invoices</h4><div class="room-list">${quotes.length?quotes.slice().reverse().map(q=>{const invoice=invoices.find(i=>i.quoteId===q.id);return `<div class="room-item billing-quote" data-quote-id="${esc(q.id)}"><b>${esc(q.quoteNumber)} · ${esc(q.title)}</b><small>${money(q.amount)} total · ${money(q.depositAmount)} deposit · ${esc(q.status)}</small>${q.description?`<p>${esc(q.description)}</p>`:''}${invoice?`<small>Invoice ${esc(invoice.invoiceNumber)} · Paid ${money(invoice.amountPaid)} · Balance ${money(invoice.balance)} · ${esc(invoice.status)}</small>`:''}<div class="billing-actions">${client&&q.status==='Awaiting client approval'?`<button class="button compact quote-approve">Approve</button><button class="button compact dark quote-decline">Decline</button>`:''}${client&&['Approved — payment due','Part-paid'].includes(q.status)?`<button class="button compact quote-pay-deposit">Pay deposit</button><button class="button compact dark quote-pay-full">Pay full balance</button>`:''}</div><p class="status"></p></div>`}).join(''):'<p class="room-empty">No professional fee quotes yet.</p>'}</div></div><details class="billing-ledger"><summary>Payment ledger</summary><div class="room-list">${payments.length?payments.slice().reverse().map(p=>`<div class="room-item"><b>${money(p.amount)} · ${esc(p.status)}</b><small>${esc(p.paymentType||p.kind||'Case payment')} · ${esc(p.mode||'')}</small></div>`).join(''):'<p class="room-empty">No case payments recorded yet.</p>'}</div></details></section>`}
function outcomeMarkup(o,caseId,role){const pro=role==='professional'||role==='admin',client=role==='client',x=o?.outcome;return `<section class="case-room-panel case-billing-panel"><div class="case-room-head billing-head"><div><p class="eyebrow">Case closure & outcome</p><h3>Measure the result.</h3><p class="room-role-note">Only client-confirmed outcome figures count toward The Chancellor's impact totals.</p></div>${x?`<span class="badge">${esc(x.status)}</span>`:''}</div>${x?`<div class="billing-summary"><span><small>Value saved</small><b>${money(x.valueSaved)}</b></span><span><small>Debt reduced</small><b>${money(x.debtReduced)}</b></span><span><small>Time to resolution</small><b>${Number(x.daysToResolution||0)} days</b></span></div><div class="room-item"><b>${esc(x.outcomeType)}</b><p>${esc(x.resolutionSummary)}</p><small>Before ${money(x.amountBefore)} · After ${money(x.amountAfter)}</small><small>Asset protected ${money(x.assetValueProtected)} · Tax exposure reduced ${money(x.taxExposureReduced)} · Cash-flow improvement ${money(x.cashFlowImprovement)} · Jobs protected ${Number(x.jobsProtected||0)}</small></div>${client&&!x.clientConfirmed?`<form class="room-form room-outcome-confirm"><label><input type="checkbox" name="confirm" value="yes" required> I confirm that this outcome fairly reflects the result of my case.</label><button class="button compact">Confirm outcome</button><p class="status"></p></form>`:''}`:pro?`<form class="room-form room-outcome-form"><select name="outcomeType" required><option value="">Choose outcome</option><option>Debt reduced / restructured</option><option>Tax exposure reduced / settled</option><option>Asset protected</option><option>Business stabilised / turnaround</option><option>Legal matter resolved</option><option>Payment arrangement achieved</option><option>Other positive resolution</option></select><textarea name="resolutionSummary" rows="4" required placeholder="What was achieved for the client?"></textarea><div class="billing-two"><input name="amountBefore" type="number" min="0" step="0.01" placeholder="Amount / exposure before"><input name="amountAfter" type="number" min="0" step="0.01" placeholder="Amount / exposure after"></div><div class="billing-two"><input name="valueSaved" type="number" min="0" step="0.01" placeholder="Value saved"><input name="debtReduced" type="number" min="0" step="0.01" placeholder="Debt reduced"></div><div class="billing-two"><input name="assetValueProtected" type="number" min="0" step="0.01" placeholder="Asset value protected"><input name="taxExposureReduced" type="number" min="0" step="0.01" placeholder="Tax exposure reduced"></div><div class="billing-two"><input name="cashFlowImprovement" type="number" min="0" step="0.01" placeholder="Monthly cash-flow improvement"><input name="jobsProtected" type="number" min="0" step="1" placeholder="Jobs protected"></div><button class="button compact">Record case outcome & resolve case</button><p class="status"></p></form>`:'<p class="room-empty">The assigned professional will record the final outcome when the intervention is complete.</p>'}</section>`}
function markup(room,caseId,role,billing,outcome){const pro=role==='professional'||role==='admin';return `<div class="case-room-head"><div><p class="eyebrow">Secure Client ↔ Professional Case Room</p><h2>Case collaboration</h2><p class="room-role-note">Messages and files in this room belong to this Rescue case and form part of its activity record.</p></div><span class="badge">${esc(role)}</span></div><div class="case-room-grid"><section class="case-room-panel"><h3>Messages</h3><div class="room-thread">${(room.messages||[]).length?(room.messages||[]).map(m=>`<article class="room-message ${esc(String(m.authorRole||'').toLowerCase())}"><small>${esc(m.authorName)} · ${when(m.createdAt)}</small><p>${esc(m.message)}</p></article>`).join(''):'<p class="room-empty">No messages yet.</p>'}</div><form class="room-form room-message-form"><textarea name="message" rows="3" maxlength="2500" required placeholder="Write a case message…"></textarea><button class="button compact">Send message</button><p class="status"></p></form></section><section class="case-room-panel"><h3>Documents</h3><div class="room-list">${(room.documents||[]).length?(room.documents||[]).map(d=>`<div class="room-item"><b>${esc(d.documentType)}</b><small>${esc(d.originalName)} · ${esc(d.uploadedByName||d.uploadedByRole)}</small><a href="/api/case-room/${encodeURIComponent(caseId)}/documents/${encodeURIComponent(d.id)}/download">Open securely →</a></div>`).join(''):'<p class="room-empty">No case documents yet.</p>'}</div><form class="room-form room-document-form" enctype="multipart/form-data"><select name="documentType"><option>Notice / demand / assessment</option><option>Agreement / contract</option><option>Statement / account</option><option>Court / legal document</option><option>Tax document</option><option>Identity / authority document</option><option>Supporting evidence</option></select><input type="file" name="documents" accept=".pdf,.jpg,.jpeg,.png" multiple required><button class="button compact">Upload documents</button><p class="status"></p></form></section></div><div class="room-columns"><section class="case-room-panel"><h3>Milestones</h3><div class="room-list">${(room.milestones||[]).length?(room.milestones||[]).map(m=>`<div class="room-item"><b>${esc(m.title)}</b><small>${esc(m.status)}${m.dueAt?' · Due '+when(m.dueAt):''}</small>${m.note?`<small>${esc(m.note)}</small>`:''}</div>`).join(''):'<p class="room-empty">No milestones yet.</p>'}</div>${pro?`<form class="room-form room-milestone-form"><input name="title" required placeholder="Milestone"><select name="status"><option>Open</option><option>In progress</option><option>Waiting on client</option><option>Waiting on third party</option><option>Complete</option></select><input name="dueAt" type="datetime-local"><textarea name="note" rows="2" placeholder="Milestone note"></textarea><button class="button compact">Add milestone</button><p class="status"></p></form>`:''}</section><section class="case-room-panel"><h3>Appointments</h3><div class="room-list">${(room.appointments||[]).length?(room.appointments||[]).map(a=>`<div class="room-item"><b>${esc(a.title)}</b><small>${when(a.scheduledAt)} · ${esc(a.mode)} · ${esc(a.status)}</small>${a.note?`<small>${esc(a.note)}</small>`:''}</div>`).join(''):'<p class="room-empty">No appointments scheduled.</p>'}</div>${pro?`<form class="room-form room-appointment-form"><input name="title" required placeholder="Appointment / consultation"><input name="scheduledAt" type="datetime-local" required><select name="mode"><option>Online / phone</option><option>In person</option><option>Video meeting</option></select><textarea name="note" rows="2" placeholder="Appointment note"></textarea><button class="button compact">Schedule appointment</button><p class="status"></p></form>`:''}</section></div>${billingMarkup(billing||{},caseId,role)}${outcomeMarkup(outcome||{},caseId,role)}<section class="case-room-panel"><h3>Activity trail</h3><div class="room-list room-activity">${(room.activity||[]).length?(room.activity||[]).slice().reverse().map(a=>`<div class="room-item"><b>${esc(a.action)}</b><small>${esc(a.actorName)} · ${when(a.createdAt)}</small>${a.detail?`<small>${esc(a.detail)}</small>`:''}</div>`).join(''):'<p class="room-empty">Activity will appear here as the case progresses.</p>'}</div></section>`}
async function request(url,options){const r=await fetch(url,options);const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Case room action could not be completed.');return j}
function openPayment(j){const form=document.createElement('form');form.method='post';form.action=j.url;Object.entries(j.fields||{}).forEach(([name,value])=>{const input=document.createElement('input');input.type='hidden';input.name=name;input.value=value;form.appendChild(input)});document.body.appendChild(form);form.submit()}
async function mount(el,{caseId,role='client',endpoint,onRefresh}){const data=await request(endpoint);const id=caseId||data.caseId;const [billing,outcome]=await Promise.all([request(`/api/case-room/${encodeURIComponent(id)}/billing`).catch(()=>({})),request(`/api/case-room/${encodeURIComponent(id)}/outcome`).catch(()=>({}))]);el.innerHTML=markup(data.room||{},id,role,billing,outcome);el.hidden=false;const refresh=()=>onRefresh?onRefresh():mount(el,{caseId:id,role,endpoint});const msg=el.querySelector('.room-message-form');if(msg)msg.onsubmit=async e=>{e.preventDefault();const s=msg.querySelector('.status');s.textContent='Sending…';try{await request(`/api/case-room/${encodeURIComponent(id)}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(msg)))});refresh()}catch(err){s.textContent=err.message}};const doc=el.querySelector('.room-document-form');if(doc)doc.onsubmit=async e=>{e.preventDefault();const s=doc.querySelector('.status');s.textContent='Uploading…';try{await request(`/api/case-room/${encodeURIComponent(id)}/documents`,{method:'POST',body:new FormData(doc)});refresh()}catch(err){s.textContent=err.message}};const mil=el.querySelector('.room-milestone-form');if(mil)mil.onsubmit=async e=>{e.preventDefault();const s=mil.querySelector('.status');s.textContent='Saving…';try{await request(`/api/case-room/${encodeURIComponent(id)}/milestones`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(mil)))});refresh()}catch(err){s.textContent=err.message}};const ap=el.querySelector('.room-appointment-form');if(ap)ap.onsubmit=async e=>{e.preventDefault();const s=ap.querySelector('.status');s.textContent='Scheduling…';try{await request(`/api/case-room/${encodeURIComponent(id)}/appointments`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(ap)))});refresh()}catch(err){s.textContent=err.message}};const quote=el.querySelector('.room-quote-form');if(quote)quote.onsubmit=async e=>{e.preventDefault();const s=quote.querySelector('.status');s.textContent='Issuing quote…';try{await request(`/api/case-room/${encodeURIComponent(id)}/quotes`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(quote)))});refresh()}catch(err){s.textContent=err.message}};const outcomeForm=el.querySelector('.room-outcome-form');if(outcomeForm)outcomeForm.onsubmit=async e=>{e.preventDefault();const s=outcomeForm.querySelector('.status');s.textContent='Recording outcome…';try{await request(`/api/case-room/${encodeURIComponent(id)}/outcome`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(outcomeForm)))});refresh()}catch(err){s.textContent=err.message}};const outcomeConfirm=el.querySelector('.room-outcome-confirm');if(outcomeConfirm)outcomeConfirm.onsubmit=async e=>{e.preventDefault();const s=outcomeConfirm.querySelector('.status');s.textContent='Confirming…';try{await request('/api/rescue/case-outcome/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirm:'yes'})});refresh()}catch(err){s.textContent=err.message}};el.querySelectorAll('.billing-quote').forEach(card=>{const qid=card.dataset.quoteId,s=card.querySelector('.status'),decision=async d=>{s.textContent=`${d}ing…`;try{await request(`/api/case-room/${encodeURIComponent(id)}/quotes/${encodeURIComponent(qid)}/decision`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({decision:d})});refresh()}catch(err){s.textContent=err.message}},pay=async full=>{s.textContent='Preparing secure payment…';try{const j=await request(`/api/case-room/${encodeURIComponent(id)}/quotes/${encodeURIComponent(qid)}/checkout`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({payFull:full})});openPayment(j)}catch(err){s.textContent=err.message}};card.querySelector('.quote-approve')?.addEventListener('click',()=>decision('Approve'));card.querySelector('.quote-decline')?.addEventListener('click',()=>decision('Decline'));card.querySelector('.quote-pay-deposit')?.addEventListener('click',()=>pay(false));card.querySelector('.quote-pay-full')?.addEventListener('click',()=>pay(true));});return data}
window.CaseRoom={mount};})();