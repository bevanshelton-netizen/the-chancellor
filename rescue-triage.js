const allowedFlags=new Set(['formal_notice','summons','repossession','garnishee','tax_debt','business_payroll']);
function finiteNonNegative(value,max=Number.MAX_SAFE_INTEGER){const n=Number(value||0);return Number.isFinite(n)?Math.max(0,Math.min(max,n)):0;}
function scoreRescueTriage(input={}){const type=String(input.type||'').toLowerCase();const amount=finiteNonNegative(input.amount,1e12),income=finiteNonNegative(input.income,1e12),expenses=finiteNonNegative(input.expenses,1e12);const days=input.days===''||input.days==null?null:Math.round(finiteNonNegative(input.days,3650));const flags=Array.isArray(input.flags)?[...new Set(input.flags.map(x=>String(x||'').trim()).filter(x=>allowedFlags.has(x)))].slice(0,12):[];let score=15;const d=days==null?999:days;if(d<=3)score+=35;else if(d<=7)score+=28;else if(d<=14)score+=20;else if(d<=30)score+=12;if(flags.includes('summons'))score+=30;if(flags.includes('repossession'))score+=30;if(flags.includes('garnishee'))score+=25;if(flags.includes('formal_notice'))score+=15;if(flags.includes('business_payroll'))score+=18;if(flags.includes('tax_debt'))score+=8;if(income&&expenses>=income)score+=15;if(income&&amount>income*6)score+=12;score=Math.min(100,score);const band=score>=80?'Critical':score>=60?'High':score>=35?'Moderate':'Low';const priorities=[];if(flags.includes('summons'))priorities.push('Court papers detected: professional legal review should be treated as urgent.');if(flags.includes('repossession'))priorities.push('Asset at risk: preserve every notice, finance agreement and payment record; urgent professional review is recommended.');if(flags.includes('garnishee'))priorities.push('Attachment/deduction issue: gather the order, account statements and employment/bank records for review.');if(type==='tax'||flags.includes('tax_debt'))priorities.push('Tax matter: assemble assessments, returns, statements of account and all revenue-service correspondence for a registered tax professional.');if(type==='debt')priorities.push('Debt pressure: map every creditor, balance, instalment, arrears amount and interest rate before considering a restructuring route.');if(type==='business')priorities.push('Business distress: protect cash flow first by mapping payroll, tax, secured debt, creditors, debtors and 13-week cash needs.');if(type==='legal')priorities.push('Legal matter: do not ignore deadlines or sign admissions or settlements you do not understand; obtain qualified legal review.');if(income&&expenses>=income)priorities.push('Stated essential expenses meet or exceed income; affordability needs review before any repayment promise.');if(!priorities.length)priorities.push('Organise the notice, agreement, statements and timeline before speaking to the relevant professional.');const specialist=type==='tax'?'registered tax practitioner':type==='debt'?'NCR-registered debt counsellor':type==='asset'?'consumer-credit/debt specialist and, where needed, an attorney':type==='business'?'turnaround specialist/accountant and relevant regulated professionals':'attorney / legal practitioner';return{score,band,specialist,priorities,amount,income,expenses,days,flags};}
module.exports={scoreRescueTriage};