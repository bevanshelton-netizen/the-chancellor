(()=>{const KEY='chancellor_institution_v1';const clean=v=>String(v||'').trim().toUpperCase().slice(0,80);const params=new URLSearchParams(location.search);const incoming=clean(params.get('institution')||params.get('benefit')||params.get('org'));if(incoming)localStorage.setItem(KEY,incoming);const code=clean(localStorage.getItem(KEY));async function post(path){if(!code)return{ok:true,linked:false};const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Institutional benefit could not be linked.');return j}window.ChancellorInstitution={code,linkAudit:()=>post('/api/institutions/link-audit'),linkCase:()=>post('/api/institutions/link-case')};})();