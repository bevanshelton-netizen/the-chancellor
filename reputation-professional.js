(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));async function load(){const dash=document.querySelector('#memberDash');if(!dash||dash.classList.contains('hidden'))return;const r=await fetch('/api/professionals/reputation');if(!r.ok)return;const d=await r.json(),p=d.performance||{};let box=document.querySelector('#performancePanel');if(!box){box=document.createElement('section');box.id='performancePanel';box.className='panel';const anchor=document.querySelector('.membership-panel');(anchor||dash).after(box)}box.innerHTML=`<div class="panel-heading"><div><p class="eyebrow">Chancellor Performance</p><h2>Your professional score</h2><p class="muted">Performance supports matching priority after verification, speciality, jurisdiction and availability.</p></div><div class="metric"><small>Performance score</small><strong>${Number(p.score||0)}</strong></div></div><div class="status-stack"><span><small>Client rating</small><b>${p.rating==null?'No ratings yet':`${p.rating}/5 (${p.ratings})`}</b></span><span><small>Acceptance rate</small><b>${p.acceptanceRate||0}%</b></span><span><small>Resolution rate</small><b>${p.resolutionRate||0}%</b></span><span><small>Average response</small><b>${p.avgResponseHours==null?'Building history':`${p.avgResponseHours} hrs`}</b></span></div><div class="credential-member-list" style="margin-top:18px">${(d.ratings||[]).slice().reverse().slice(0,5).map(x=>`<div class="credential-member-row"><div><b>${Number(x.rating)}/5 · ${esc(x.caseNumber||'Resolved case')}</b><small>${esc(x.comment||'No written comment')}</small></div></div>`).join('')||'<p class="muted">Client feedback will appear after resolved cases.</p>'}</div>`}load().catch(()=>{});setInterval(()=>load().catch(()=>{}),120000);})();