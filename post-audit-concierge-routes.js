const store=require('./store');
const {buildQuoteSuggestion}=require('./quote-engine');
const requireClient=(req,res,next)=>req.session?.clientId?next():res.status(401).json({error:'Please sign in to continue.'});
const paidForAudit=(db,auditId)=>(db.payments||[]).some(p=>p.auditId===auditId&&String(p.status||'').toUpperCase()==='COMPLETE');
function diagnosis(audit){const pct=Number(audit.readinessPercent||0);if(pct>=92)return 'Your business is operating at a high-readiness level. The next priority is disciplined expansion, investment readiness and stronger market execution.';if(pct>=78)return 'Your business is scale ready. Protect the strong fundamentals while preparing systems, capital and delivery capacity for larger opportunities.';if(pct>=56)return 'Your business is growth ready, but targeted improvements can materially improve its ability to convert opportunities and scale reliably.';if(pct>=34)return 'Your business needs stabilisation before aggressive growth. Focus management attention and cash on the weakest commercial and operating constraints first.';return 'Your audit indicates business distress. Immediate corrective implementation should come before taking on greater financial, contractual or operating risk.'}
module.exports=function registerPostAuditConcierge(app){app.get('/api/client/post-audit-concierge',requireClient,(req,res)=>{const db=store.read(),audit=(db.audits||[]).find(a=>a.id===req.session.clientId);if(!audit)return res.status(404).json({error:'Audit not found.'});if(!paidForAudit(db,audit.id))return res.status(402).json({error:'Complete the R500 audit payment before The Chancellor prepares the post-audit recommendation.'});const q=buildQuoteSuggestion(audit);const top=(audit.priorities||[]).slice(0,3).map(p=>({section:p.section,score:Number(p.score||0),maxScore:Number(p.maxScore||20),percent:Number.isFinite(Number(p.percent))?Number(p.percent):Math.round((Number(p.score||0)/Number(p.maxScore||20))*100)}));res.setHeader('Cache-Control','private, no-store');res.json({ok:true,diagnosis:diagnosis(audit),score:Number(audit.score||0),maxScore:Number(audit.maxScore||180),readinessPercent:Number(audit.readinessPercent||0),band:audit.band||'',priorities:top,recommendation:audit.recommendation||'',recommendedService:q.service,amount:Number(q.amount),tier:q.tier,tierLabel:q.tierLabel,focusAreas:q.focusAreas,why:q.description,nextAction:'Download your implementation proposal, review the recommended offer in your portal, and pay securely when you are ready to proceed.'})})};
